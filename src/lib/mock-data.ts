// This file is no longer needed as data is fetched from Firebase.
// Keeping it empty or removing it is fine.
// An empty export prevents potential import errors if other files still reference it temporarily.
export {};
